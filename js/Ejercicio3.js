/**3. Convertir a minúsculas
 * Escribe una función que convierta una cadena a minúsculas utilizando
toLowerCase().
Ejemplo: "MUNDO" → "mundo"
 */

let palabra=prompt("Escribe una cadena: ");
const longitud= texto=> texto.toLowerCase();
console.log(`La palabra en minuscula es ${longitud(palabra)}`);